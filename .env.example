# Backend API (trailing slash required)
# Local split dev (Vite :5173 + Django :8000):
# VITE_API_URL=http://127.0.0.1:8000/api/v1/
# Same host / Docker / production nginx:
VITE_API_URL=/api/v1/

# Never enable in production — mocks auth/API in the browser
# VITE_USE_FAKE_BACKEND=false
