# API base URL (use relative path when frontend is served by the same host)
VITE_API_URL=/api/v1/

# Set to "true" only for theme demo without a backend (intercepts /login)
# VITE_USE_FAKE_BACKEND=false
